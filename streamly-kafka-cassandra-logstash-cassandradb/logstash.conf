input {
  file {
  	path => "/tmp/dummyfile" # Dummy logstash input file
  }
}
filter {
  mutate {
    remove_field => [ "beat" ]
  }
}
output {
    cassandra {
        hosts => ["london201.streamly.io","london202.streamly.io","london205.streamly.io"]
        port => 9042
        keyspace => "greenspace_keyspace" # Keyspace should be prefixed by your namespace
        table => "greenspace_output2" 
        username => "l0rwjuj000wk3h2" # Username to authenticate ( your access key)
        password => "20xrhdi9rjjhj2f" # Username to authenticate ( your secret key)
    }
}